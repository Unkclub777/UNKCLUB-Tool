name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g. 1.2.0)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  build-release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Resolve version
        id: version
        shell: pwsh
        run: |
          if ("${{ github.event_name }}" -eq 'workflow_dispatch') {
            $version = "${{ inputs.version }}"
          } else {
            $version = "${{ github.ref_name }}".TrimStart('v')
          }
          "version=$version" >> $env:GITHUB_OUTPUT

      - name: Publish app (small exe, no embedded installers)
        shell: pwsh
        run: |
          dotnet publish PreInstallTool/PreInstallTool.csproj `
            -c Release `
            -r win-x64 `
            -o publish-staging `
            /p:Version=${{ steps.version.outputs.version }} `
            /p:AssemblyVersion=${{ steps.version.outputs.version }}.0 `
            /p:FileVersion=${{ steps.version.outputs.version }}.0 `
            /p:InformationalVersion=${{ steps.version.outputs.version }}

          New-Item -ItemType Directory -Path publish -Force | Out-Null
          Move-Item -Path publish-staging/PreInstallTool.exe -Destination "publish/UNKCLUB Tool.exe" -Force
          Remove-Item publish-staging -Recurse -Force -ErrorAction SilentlyContinue

      - name: Update version.json
        shell: pwsh
        run: |
          $version = "${{ steps.version.outputs.version }}"
          $repo = "${{ github.repository }}"
          $manifest = @{
            version = $version
            downloadUrl = "https://github.com/$repo/releases/download/v$version/UNKCLUB Tool.exe"
            installersBundleUrl = "https://github.com/$repo/releases/download/v$version/installers-bundle.zip"
            installersBundleVersion = $version
            releaseNotes = "Release v$version"
          } | ConvertTo-Json -Depth 3
          Set-Content -Path version.json -Value $manifest -Encoding UTF8

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: UNKCLUB Tool v${{ steps.version.outputs.version }}
          files: |
            publish/UNKCLUB Tool.exe
            version.json
            PreInstallTool/installers-bundle.zip
          fail_on_unmatched_files: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit version.json bump
        shell: pwsh
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add version.json
          git diff --cached --quiet || git commit -m "chore: bump version.json to v${{ steps.version.outputs.version }}"
          git push
